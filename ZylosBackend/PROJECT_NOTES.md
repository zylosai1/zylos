# Zylos Backend — Project Notes

## Quickstart
1. Copy `.env.template` to `.env` and update values.
2. Create and activate venv:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt